<h2><%= title %></h2>

<%= form_for(variablee) do |f| %>
  <% if variablee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(variablee.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% variablee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :mail %><br />
    <%= f.text_field :mail %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.text_field :password %>
  </div>
  <div class="field">
    <%= f.label :admin %><br />
    <%= f.check_box :admin %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
